# 📋 Deep Agents 프로젝트 수정 내역 전체 요약

**기준 커밋**: `45e61425` (메모리 저장 체계 구현 이전) → `8a107f73` (현재 HEAD)
**수정 기간**: 2026-04-06 ~ 2026-04-07

---

## 1. 수정했던 내용 요약 (6개 영역)

| # | 영역 | 내용 | 커밋 |
|---|------|------|------|
| 1 | **메모리 저장 체계** | CLI에 SQLite 기반 개발자 지식 베이스 시스템 구현 | `45e61425` |
| 2 | **동적 SubAgent 생성** | SDK에 런타임 서브에이전트 즉석 생성 기능 구현 | `3e043d14` |
| 3 | **CLI 시스템 프롬프트** | AI 에이전트 행동 지침 및 메모리 시스템 가이드 추가 | `8a107f73` |
| 4 | **Docker + 실행 스크립트** | Docker 컨테이너 실행 환경 및 `deepagents.sh` 생성 | `8a107f73` |
| 5 | **ACP 보안 강화** | 위험한 셸 패턴 자동 승인 차단 로직 추가 | `b1670136` |
| 6 | **문서화** | AGENTS.md, 각종 가이드 문서 신규 작성 | `8097a6b6`, `4448541e` |

---

## 2. 각 수정 내용 상세 설명

---

### ✅ 수정 1: 메모리 저장 체계 (Memory System)

**파일**: `libs/cli/deepagents_cli/memory_manager.py` (기존 수정)

**내용**:
- SQLite 데이터베이스(`~/.deepagents/memory/memory.db`)로 학습 내용 영구 저장
- 개발자 프로필 JSON 파일(`~/.deepagents/memory/developer_profile.json`) 관리
- `/memory add`, `/memory search`, `/memory profile`, `/memory export`, `/memory stats` 슬래시 명령 지원
- 카테고리 분류: `best_practice`, `anti_pattern`, `preference`, `knowledge`
- 소스 분류: `user_feedback`, `conversation`, `correction`, `discovery`

**생성된 문서**:
- `MEMORY_SYSTEM_GUIDE.md` (525줄) — 개발자용 상세 가이드
- `MEMORY_USAGE_GUIDE.md` (778줄) — 사용자용 사용 가이드

---

### ✅ 수정 2: 동적 SubAgent 생성 (Dynamic SubAgent Spawning)

**파일**: `libs/deepagents/deepagents/middleware/subagents.py` (대규모 수정)

**내용**:
- `SpawnAgentConfig` TypedDict 신규 추가 — 동적 서브에이전트 설정 스키마
- `DynamicSubAgentRegistry` 클래스 신규 추가 — 런타임 서브에이전트 레지스트리
- `TaskToolSchema`에 `spawn_config` 필드 추가 — 도구 스키마 확장
- 기존 `_SubagentSpec` TypedDict → `DynamicSubAgentRegistry`로 리팩터링
- 에페메럴(ephemeral) 서브에이전트: 작업 완료 후 자동 정리
- 캐싱: 동일 이름은 재사용하여 성능 최적화

**추가된 테스트**:
- `libs/deepagents/tests/unit_tests/test_dynamic_subagents.py` (326줄, 29개 테스트 케이스)

**생성된 문서**:
- `SUBAGENT_DYNAMIC_GUIDE.md` (570줄) — 동적 SubAgent 개발 가이드
- `SUBAGENT_DEMO_GUIDE.md` (282줄) — CLI 데모 가이드
- `CLI_INTEGRATION_GUIDE.md` (310줄) — CLI 통합 가이드
- `examples/simple_subagent_demo.py` (130줄) — 실행 가능한 데모 스크립트

---

### ✅ 수정 3: CLI 시스템 프롬프트

**파일**: `libs/cli/deepagents_cli/system_prompt.md` (342줄 신규 추가)

**내용**:
- AI 에이전트의 핵심 행동 지침 (간결성, 코드 품질, 안전 규칙)
- 메모리 시스템 사용법 가이드 (자동 도메인 지식 캡처 패턴)
- Git 안전 프로토콜 (강제 push 금지, 커밋 규칙)
- 도구 사용 우선순위 (`read_file` > `cat`, `edit_file` > `sed` 등)
- 보안 가이드 (XSS, SQL injection 방지)
- 서브에이전트 위임 패턴

---

### ✅ 수정 4: Dockerfile + deepagents.sh

**파일**: `Dockerfile` (25줄 신규), `deepagents.sh` (57줄 신규)

**Dockerfile 내용**:
- 기반 이미지: `python:3.12-slim`
- 시스템 도구 설치: `git`, `curl`, `ripgrep`
- `uv`를 통한 Deep Agents CLI 설치
- `/workspace` 디렉토리를 작업 공간으로 설정
- `deepagents` 명령을 ENTRYPOINT로 지정

**deepagents.sh 내용**:
- Docker 이미지 자동 빌드 (없을 경우)
- 워크스페이스 경로 마운트 지원 (`./deepagents.sh /path/to/workspace`)
- 메모리 DB 영속성 마운트 (`~/.deepagents` → `/root/.deepagents`)
- MCP 설정 파일 자동 마운트 (`~/.mcp.json`)
- API 키 환경 변수 자동 전달 (Anthropic, OpenAI, OpenRouter, Tavily)

---

### ✅ 수정 5: ACP 보안 강화

**파일**: `libs/acp/deepagents_acp/server.py`, `libs/acp/deepagents_acp/utils.py`

**내용**:
- `contains_dangerous_patterns()` 함수 추가
- 위험한 셸 메타문자(`$()`, 백틱, `;`, 리다이렉션 등)가 포함된 명령어는 자동 승인 차단
- 기존에 사용자가 승인한 명령 유형이라도 셸 인젝션 패턴 포함 시 재승인 요구
- 관련 테스트: `libs/acp/tests/test_dangerous_patterns.py` (89줄)

---

### ✅ 수정 6: AGENTS.md 및 .mcp.json 정리

**파일**: `AGENTS.md` (288줄 추가), `.mcp.json` (삭제 후 전역으로 이동)

**AGENTS.md 내용**:
- 전체 프로젝트 개발 지침 문서
- 코드 품질 기준, 테스트 요구사항, 보안 규칙
- CLI 패키지별 개발 가이드라인
- `.mcp.json`: 프로젝트 루트에서 `~/.mcp.json`(전역)으로 이동

---

## 3. 각 수정사항을 재현하기 위한 프롬프트

---

### 🔧 프롬프트 1: 메모리 시스템 구현

```
Deep Agents CLI에 개발자 개인 지식 베이스(Personal Knowledge Base) 메모리 시스템을 구현해주세요.

요구사항:
1. SQLite 데이터베이스를 사용하여 학습 내용을 영구 저장 (경로: ~/.deepagents/memory/memory.db)
2. 개발자 프로필을 JSON으로 저장 (경로: ~/.deepagents/memory/developer_profile.json)
3. libs/cli/deepagents_cli/memory_manager.py에 MemoryManager 클래스 구현
4. 다음 슬래시 명령어 지원:
   - /memory add "내용" - 학습 추가
   - /memory search "쿼리" - 검색
   - /memory profile - 프로필 조회/수정
   - /memory export - JSON 내보내기
   - /memory stats - 통계 조회
5. 카테고리 분류: best_practice, anti_pattern, preference, knowledge
6. 소스 분류: user_feedback, conversation, correction, discovery
7. libs/cli/deepagents_cli/command_registry.py에 /memory 명령 등록
8. libs/cli/deepagents_cli/app.py에서 명령 처리 로직 추가

참고: 기존 코드 패턴을 따르고 type hints를 사용하세요.
```

---

### 🔧 프롬프트 2: 동적 SubAgent Spawning 구현

```
Deep Agents SDK에 동적 서브에이전트 생성(Dynamic SubAgent Spawning) 기능을 구현해주세요.

파일: libs/deepagents/deepagents/middleware/subagents.py

요구사항:
1. SpawnAgentConfig TypedDict 추가 - 동적 서브에이전트 설정 스키마
   - 필수 필드: name(str), role(str), instructions(str), tools(list[str])
   - 선택 필드: model(str) - 부모 에이전트 모델 상속

2. DynamicSubAgentRegistry 클래스 구현
   - spawn(config: SpawnAgentConfig) -> SubAgent 메서드
   - get(name: str) -> SubAgent | None 메서드
   - list_active() -> list[str] 메서드
   - 동일 이름 서브에이전트 캐싱 지원

3. TaskToolSchema 확장
   - 기존 subagent_type 필드 유지 (기본값 "" 설정)
   - spawn_config: SpawnAgentConfig | None 필드 추가 (기본값 None)
   - spawn_config가 있으면 subagent_type보다 우선

4. _build_task_tool() 및 task/atask 함수에서 spawn_config 처리 로직 추가

5. 단위 테스트 작성: libs/deepagents/tests/unit_tests/test_dynamic_subagents.py
   - SpawnAgentConfig 유효성 검사 테스트
   - DynamicSubAgentRegistry 동작 테스트
   - task 도구 통합 테스트
   - 총 29개 이상의 테스트 케이스

모든 type hints 포함, Google 스타일 docstring 사용.
```

---

### 🔧 프롬프트 3: CLI 시스템 프롬프트 파일 생성

```
Deep Agents CLI의 AI 에이전트 시스템 프롬프트 파일을 생성해주세요.

파일: libs/cli/deepagents_cli/system_prompt.md

포함해야 할 섹션:
1. Core Behavior - 간결성, 불필요한 설명 금지, 진행 상황 업데이트
2. Professional Objectivity - 기술적 정확성 우선
3. Following Conventions - 기존 코드 스타일 준수
4. Doing Tasks - 4단계 작업 절차 (이해→구현→테스트→검증)
5. Tool Usage - 전문 도구 우선 (read_file>cat, edit_file>sed)
6. Git Safety Protocol - 강제 push 금지, 커밋 규칙
7. Security - XSS/SQL injection/커맨드 인젝션 방지
8. Developer Memory System 섹션:
   - 메모리 저장 위치 안내
   - 메모리 업데이트 시점 가이드
   - /memory 명령어 사용법
   - 자동 도메인 지식 캡처 패턴 (프로젝트 설명, 비즈니스 규칙, 도메인 용어, API 엔드포인트, 기술 스택, 팀 워크플로우)
9. Skills Directory 섹션
10. Human-in-the-Loop Tool Approval 섹션
11. Todo List Management 섹션

템플릿 변수: {mode_description}, {interactive_preamble}, {ambiguity_guidance}, {model_identity_section}, {working_dir_section}, {skills_path}
```

---

### 🔧 프롬프트 4: Docker 환경 구성

```
Deep Agents CLI를 Docker 컨테이너로 실행할 수 있도록 Dockerfile과 실행 스크립트를 생성해주세요.

파일 1: Dockerfile (프로젝트 루트)
- 기반 이미지: python:3.12-slim
- 필수 패키지: git, curl, ripgrep
- uv 설치 (pip install uv)
- libs/ 디렉토리 복사 후 uv pip install --system -e /app/libs/cli
- WORKDIR: /workspace
- ENV TERM=xterm-256color
- ENTRYPOINT: ["deepagents"]

파일 2: deepagents.sh (프로젝트 루트)
- 사용법: ./deepagents.sh [workspace_path]
- 첫 번째 인자를 워크스페이스로 사용, 없으면 현재 디렉토리
- Docker 이미지(deepagents-cli)가 없으면 자동 빌드
- 마운트 설정:
  - 워크스페이스: $WORKSPACE → /workspace
  - 메모리 DB: ~/.deepagents → /root/.deepagents (mkdir -p로 생성)
  - MCP 설정: ~/.mcp.json → /root/.mcp.json:ro (파일 존재 시만)
- 환경변수 자동 전달: ANTHROPIC_API_KEY, OPENAI_API_KEY, OPENROUTER_API_KEY, DEEPAGENTS_DEFAULT_MODEL, TAVILY_API_KEY
- docker run -it --rm --name deepagents-session으로 실행
```

---

### 🔧 프롬프트 5: ACP 보안 강화 (위험한 셸 패턴 차단)

```
ACP 서버의 명령어 자동 승인 로직에 보안 취약점이 있습니다. 위험한 셸 패턴을 포함한 명령어는 자동 승인되지 않도록 수정해주세요.

파일 1: libs/acp/deepagents_acp/utils.py
- contains_dangerous_patterns(command: str) -> bool 함수 추가
- 감지할 위험 패턴:
  - 커맨드 치환: $(...), `...` (백틱)
  - 명령어 연결: ; (세미콜론)
  - 리다이렉션: >, >>, <, |
  - 기타 위험 메타문자

파일 2: libs/acp/deepagents_acp/server.py
- 자동 승인 로직 수정:
  - execute 도구 명령어에 contains_dangerous_patterns() 검사 추가
  - 위험 패턴 포함 시 auto-approve 건너뛰고 사용자 승인 요구
  - utils에서 contains_dangerous_patterns import

파일 3: libs/acp/tests/test_dangerous_patterns.py (신규 테스트)
- 위험 패턴 감지 테스트
- 안전한 명령어 통과 테스트
- 다양한 엣지 케이스 포함

참고: PR #2308 스타일로 구현
```

---

### 🔧 프롬프트 6: AGENTS.md 전체 개발 지침 작성

```
Deep Agents 모노레포의 전체 개발 지침을 담은 AGENTS.md 파일을 작성해주세요.

파일: AGENTS.md (프로젝트 루트)

포함할 섹션:
1. 프로젝트 아키텍처 (모노레포 구조, 개발 도구)
2. 코드 품질 기준 (type hints 필수, 변수명 규칙, 함수 단일 책임)
3. 테스트 요구사항 (unit_tests/, integration_tests/, pytest 사용)
4. 보안 규칙 (eval/exec/pickle 금지, 예외 처리)
5. 문서화 기준 (Google 스타일 docstring, Args/Returns/Raises 섹션)
6. 패키지별 가이드라인:
   - CLI (libs/cli/): Textual TUI 프레임워크, 슬래시 명령, 시작 성능
   - SDK (libs/deepagents/): 공개 API 안정성 유지
   - ACP (libs/acp/): 보안 패턴
   - Evals (libs/evals/): 벤더 데이터 파일 무결성
7. 명명 규칙 (Python: snake_case, 상수: UPPER_SNAKE_CASE, 클래스: PascalCase)
8. 커밋 컨벤션 (Conventional Commits 형식)
9. PR 가이드라인 (AI 기여 명시, 변경 이유 설명)

중요: Sphinx 스타일 더블 백틱(``code``) 대신 단일 백틱(`code`) 사용
```

---

## 📌 중요 참고사항

- 수정 1~2는 핵심 기능 구현이며, 수정 3~4는 이를 실제로 실행 가능하게 만드는 인프라입니다
- 수정 5는 별도의 보안 버그픽스 커밋(`b1670136`)에서 시작된 내용이 통합된 것입니다
- `.mcp.json`이 프로젝트 루트에서 삭제되고 전역(`~/.mcp.json`)으로 이동된 것은 Docker 환경과의 호환성 때문입니다
- Sandbox 파일 읽기 개선(`sandbox.py`)은 원본 업스트림 커밋(`4a37a469`)에서 온 것으로, 출력 크기 제한(500KB) 및 이진 파일 처리 개선이 포함됩니다
